node_modules
dist
.env
*.local

# Signierung: der Upload-Keystore und sein Passwort dürfen NIE ins Repo.
# Geht der Keystore verloren, lässt sich die App im Play Store nicht mehr
# aktualisieren — Sicherungskopie außerhalb des Projekts anlegen.
*.keystore
*.jks
android/keystore.properties

# Cloud Functions
functions/lib/

# macOS / Build-Artefakte — gehören nicht ins Repo
.DS_Store
tsconfig.tsbuildinfo
